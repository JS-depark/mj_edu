# 백분위와 별의 비교 기준

## 데이터

- 기준 ID: `keep-shapes-136-20260922-v1`
- 생성: 2026-09-22 06:38:29 UTC
- 12개 역 각각 10,000판, 총 120,000판. 클리어 118,181판.
- 패산 136장, 공급대 13자리, 목표가 있는 네 몸통과 머리.
- 자동 플레이 정책: `mechanical-goal-v2-all12+keep-shapes-v1`.
- 목표에 맞는 묶음을 먼저 등록하고 공급한다. 타패는 목표에 필요한 패와 쌍·연속 모양을 보존하며 동률은 무작위로 선택한다. 미래 패산을 읽거나 재조립하지 않는다. 플레이어는 재조립을 사용할 수 있다.
- 원본 점수 규칙: `experiment-han-v1-all12`. 현재 게임 점수 v4와 클리어 118,181건 전부를 대조해 목표·추가 역 목록과 점수의 일치를 확인했다.
- 원본 NDJSON SHA-256: `96fbd874b04014cb7d062648dc6ddd58fcacb4428310af55a27e72fbc893604f`.

게임에 포함하는 것은 `benchmark-data.js`의 **역별 점수·빈도 집계**뿐이다. 시뮬레이터, 원본 판별 기록, 실험 보고서는 게임 및 Git 대상에 넣지 않는다. 원본은 별도 실험 폴더의 `all-yaku/keep-tiles/keep-shapes-136.ndjson`이다. 배포 때 해당 실험 폴더는 필요하지 않다.

## 계산

같은 역의 **클리어한 자동 플레이 기록만** 비교한다. 실패를 0점으로 포함하지 않으며, 사람이 플레이한 순위로 표시하지 않는다.

플레이어보다 높은 점수의 기록 수를 H, 같은 점수를 E, 클리어 표본 수를 N이라 하면:

`상위 비율(%) = (H + E / 2) / N × 100`

백분위는 `100 − 상위 비율`이다. 동점은 가운데 순위로 처리하고 별은 표시용 반올림 전의 값으로 정한다. 기준 최고점 초과를 포함해 상위 비율 1% 미만은 ‘상위 1% 미만’으로 표시한다. 이는 관측된 표본 내 위치이며 극단 점수의 정확한 장기 확률을 추정하지 않는다.

| 획득 별 | 조건 |
|---|---|
| 1개 | 목표를 완성하여 클리어 |
| 2개 | 클리어 점수가 상위 50% 이내 |
| 3개 | 클리어 점수가 상위 10% 이내 |

현재 점수는 10점 단위다. ‘다음 별까지’는 이 단위에서 다음 별 조건을 충족하는 최소 점수와 현재 점수의 차이다. 최고점은 점수 버전별, 획득 별은 비교 기준 ID별로 보존한다. 튜토리얼과 실패한 판에는 점수·백분위·별을 부여하지 않는다.

점수 버전이나 규칙 버전이 다른 판은 비교하지 않는다. 기존 108장 저장 판은 이전 규칙과 점수 v3로 끝까지 이어 할 수 있지만 새 별·순위는 받지 않는다. 새 판부터 136장과 v4를 적용한다. 이후 공급·등록·재조립 또는 점수 규칙을 바꾸면 기존 기준을 그대로 쓰지 말고 규칙 버전과 집계 데이터의 호환성을 다시 검증해야 한다.

## 해석

역마다 분포를 따로 사용하므로 동일 점수라도 순위가 다르다. 따라서 별은 **그 역에서의 자동 플레이 대비 성과**이며, 역 사이의 절대 난도나 실전 마작에서 해당 역을 노려야 할 빈도는 의미하지 않는다. 별 세 개가 모든 역에서 비슷한 비율의 기준 기록에 주어지는 것은 의도된 정규화다. 실제 플레이어의 별 획득률은 별도로 확인해야 한다.
